{
  "name": "brv-hub",
  "version": "1.0.0",
  "description": "Community-driven hub for AI coding agent content",
  "private": true,
  "scripts": {
    "validate": "node scripts/validate-manifest.js && node scripts/validate-structure.js",
    "update-registry": "node scripts/update-registry.js",
    "test": "node scripts/validate-manifest.js && node scripts/validate-structure.js"
  },
  "devDependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^3.0.1",
    "glob": "^10.3.10"
  }
}
